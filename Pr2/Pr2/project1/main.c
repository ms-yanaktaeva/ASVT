#include <8051.h>

void main(void)
{
    // ???? ??? ??????????????? ?????????? ? ????? ??????
    unsigned char massiv[10] = {
        0xC0, // 0
        0xF9, // 1
        0xA4, // 2
        0xB0, // 3
        0x99, // 4
        0x92, // 5
        0x82, // 6
        0xF8, // 7
        0x80, // 8
        0x90  // 9
    };

    unsigned char current = 9;  // ???????? ? 9

    P3 |= 0x02;                 // ??????????? P3.1 ??? ???? (???????? ? Vcc)
    P2 = massiv[current];       // ??????? ????????? ?????

    while(1)
    {
        // ????????? ??????? ?????? ?? P3.1 (???????? ?????? ???????)
        if((P3 & 0x02) == 0)
        {
            // ????????? ???????? ??????????
            if(current == 0)
                current = 9;
            else
                current--;

            P2 = massiv[current];   // ????????? ?????????

            // ???? ?????????? ??????
            while((P3 & 0x02) == 0);
        }
    }
}